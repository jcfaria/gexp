# Version control
^\.git$

# R CMD check artefacts (avoid Windows lock/permission errors)
^.*\.Rcheck$

# Tarballs generated by R CMD build (should not be nested inside the package)
^.*\.tar\.gz$

# Prebuilt vignette HTML (built locally with rmarkdown::render)
^vignettes/.*\.html$
